# 2026-09-08

## lunch-requirement 项目重构（现名 pr/p20260908）

- 修复 PRD 页样式失效：`plan.css` 选择器作用域 `.doc-page/.doc-hero/.doc-toc` 与文档实际类名（`.page/.hero/.toc`）不匹配，已全部对齐；并补上 Tailwind preflight 重置掉的 `ul/ol` 列表符号（`list-style: disc/decimal`）。
- 项目规范化命名（用户要求用日期编号，类似 `pr/p20260908`）：
  - 页面：`src/pages/pr/p20260908/`（index.astro 项目入口 / plan.astro PRD / pricing.astro 合作方案报价）
  - 资源：`src/projects/p20260908/`（plan.html 原始 PRD 内容、plan.css、pricing.css、reference/ 参考原稿）
- `PlanLayout.astro` 泛化为 `ProjectLayout.astro`：props（title/description/crumb/foot）+ `slot="actions"` 顶栏动作；页面样式由各页自行 import。
- 新增 pricing 页：由 `reference/jiagefangan.html` 移植，类名加 `pq-` 前缀避免冲突，档位联动/打印 PDF/复制邮箱交互保留，CONFIG 在 pricing.astro 脚本内。
- 旧路径 `/lunch-requirement` 保留 meta refresh 跳转页。
- `/pr/p20260908`（无子路径）入口页即跳转页：meta refresh + JS location.replace 双保险，直达 `/pr/p20260908/plan`（用户要求，原 hub 页已替换）。

## 项目约定（重要）

- **项目页命名规范**：`src/pages/pr/<编码>/`，编码 = `p + YYYYMMDD`（立项日期），如 `pr/p20260908`；对应资源放 `src/projects/<编码>/`。后续新项目按此规则加入。
- 配色/字体一律用 global.css `@theme` 的 `--color-*` / `--font-*` token。
- `astro build` 在 WorkBuddy 环境会在 ssrMoveAssets 报 brokered fs 错误（环境限制，非代码问题）；验证用 dev server。
- BSD grep 不支持 `\|` 交替，用 `grep -E`。
- 此环境 sed -i 不可用，批量替换用 `perl -pi -e`。
